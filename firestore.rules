rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /inventory/{id} { allow read, write: if true; }
    match /movements/{id} { allow read, write: if true; }
  }
}
